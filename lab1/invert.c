#include <stdio.h>
/**
 int strand(char *s) {
  int i = 255;
  while(*s) {
	printf("s is char: %d,  and string: %s .\n",s,s);
    i = i & (int)*s;
	printf("and the i is %d\n",i);
	s+= 1;
  }
  return i;
}
int main(int argc, char **argv)
  {
   // int i;
    //for (i = argc -1; i > 0; i--){
     // printf("%s ",argv[i]);
    //}
    printf("* *       %i     **\n", argc);
	//int j = strand("123426789");
    return 0;
  }
**/
int main(int argc, char **argv) {
  int i = 2, j = 1;
 // scanf("%d %d", i, j);
 // printf("%u\n", i^j);

  return 0;

  }